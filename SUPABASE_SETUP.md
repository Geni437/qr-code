# Supabase Setup

Moved to [`docs/SETUP.md`](docs/SETUP.md) as part of a fuller local-dev
setup guide (Flutter install, codegen, tests — not just Supabase). This
file is kept only so any existing links to it don't 404.
