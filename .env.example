# Copy this file to `.env` and fill in the values from your Supabase
# project's Settings -> API page. `.env` is git-ignored.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
