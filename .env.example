# Template for `.env`. Note `.env` itself is tracked (not git-ignored) in
# this repo with placeholder values so the app runs out of the box -- edit
# `.env` directly with real values rather than copying this file.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
PUBLIC_BASE_URL=https://your-domain.example.com
