# Minimal Supabase CLI project config. The CLI isn't installed in this
# environment, so this wasn't generated by `supabase init` — install the CLI
# and run `supabase init`/`supabase link` to have it fill in the rest
# (project_id, etc.) once you create a real project. See SUPABASE_SETUP.md.

project_id = "qr-ar-platform"

[db]
port = 54322
major_version = 15

[api]
enabled = true
port = 54321

[studio]
enabled = true
port = 54323
